//var ip = "192.168.1.9"; //James' IP
var ip = "192.168.1.174"; //Change to individual IP
var path = "http://" + ip + ":5000"; //Create path with port number

module.exports = {
    ip: ip,
    path: path
}